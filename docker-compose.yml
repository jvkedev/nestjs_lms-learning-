services:
  mongodb:
    image: mongo:8.0
    container_name: nest_lms_mongodb
    restart: unless-stopped

    ports:
      - '27017:27017'

    environment:
      MONGO_INITDB_DATABASE: nest_lms

    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
